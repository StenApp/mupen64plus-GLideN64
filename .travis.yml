sudo: required
dist: trusty
language: cpp
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libsdl2-dev libfreetype6-dev libgl1-mesa-dev libglu1-mesa-dev libpng-dev pkg-config zlib1g-dev binutils-dev
script:
 - ./build.sh
